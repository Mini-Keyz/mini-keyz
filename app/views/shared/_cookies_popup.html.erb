<div class="w-full flex justify-center">
  <div
    data-controller="cookies"
    data-cookies-target="cookiesElement"
    class="fixed bottom-4 flex flex-col justify-between items-center p-2 shadow-md border border-gray-100 rounded-lg bg-white"
    style="height: 200px;"
  >
    <div class="w-full flex justify-end">
      <button
        data-action="click->cookies#close"
        type="button"
        class="close h-full"
        data-dismiss="notice"
        aria-label="Close"
      >
        <span aria-hidden="true" class="text-xs p-3">&#10005;</span>
      </button>
    </div>
  
    <div>
      <p class="text-sm">Nous utilisons les cookies pour vous offrir la meilleure expérience possible sur notre application.</p>
      <br>
      <p class="text-sm">Pour plus d'informations vous pouvez consulter <%= link_to 'notre politique de cookies', cookies_policy_path, class: "font-semibold text-blue-400" %></p>
    </div>
  
    <div class="flex justify-between">
      <%= link_to 'Accepter', accept_cookies_path, remote: true, data: {'action': 'click->cookies#close'}, class: "bg-green-500 rounded p-6" %>
      <%= link_to 'Refuser', refuse_cookies_path, remote: true, data: {'action': 'click->cookies#close'} %>
    </div>
  </div>
</div>